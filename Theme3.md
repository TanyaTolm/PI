# Тема 3. Операторы, условия, циклы
Отчет по Теме #2 выполнила:
- Толмачева Татьяна Сергеевна
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |  |
| Задание 7 | + |  |
| Задание 8 | + |  |
| Задание 9 | + |  |
| Задание 10 | + |  |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа

### №1
### Создайте две переменные, значение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется».

### Ответ:
```python
one = int(input("Введите значение первой переменной: "))
two = int(input("Введите значение второй переменной: "))
if one >= two:
    print("Выполняется")
else:
    print("Не выполняется")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/1.png)

### Вывод: В даннном коде у пользователя запрашиваются две переменные, после чего они сравниватся и на консоль выводится вывод: выполняется ли условие из кода или нет.

### №2
### Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else.

### Ответ:
```python
one = int(input("Введите значение переменной: "))
if one < 0:
    print("Переменная меньше 0")
elif 0 < one < 10: print ("Переменная больше 0 и меньше 10")

else:
    print("Переменная больше 10")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/2.png)

### Вывод: В данном коде в консоль вводится число, после чего оно проходить через условие и если одно из них выполняется, то соответствующее сообщение выводится в консоли

### №3
### Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.

### Ответ:
```python
numbers = [1, 3, 4, 6, 8, 9]
value = int(input("Введите знаение переменной: "))
if value in numbers:
    print("Переменная есть в данном массиве")
else:
    print("Переменной нет в этом массиве")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/3.png)

### Вывод: В данном коде показано как происходит поиск элемента в массиве и его сравнение с переменной с помощью оператора in.

### №4
### Напишите программу, которая будет определять находится ли переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными значениями переменной value.

### Ответ:
```python
numbers = [1, 3, 4, 6, 8, 9, 15, 16, 73, 321, 322]
value = int(input("Введите знаение переменной: "))
if value in numbers:
    if value % 2 == 0:
        print("Переменная четная и есть в массиве numbers")
    else:
        print("Переменная нечетная и есть в массиве numbers")
else:
    print(f"Переменной нет в массиве numbers и она равна {value}")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/4.png)

### Вывод: В данном коде мы снова взаимодействуем с оператором in, а также проверяем четное ли число, которое мы ввели или нет. И выводим соответствующий ответ.

### №5
### Напишите программу, в которой циклом for значения переменной і будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.
### Ответ:
```python
for i in range(10):
    print ("i = ", i)
    if i == 0:
        i += 2
    if i == 1:
        continue
    if i == 2 or i == 3:
        print("Переменная равна 2 или 3")
    elif i in [4, 5, 6]:
        print("Переменная равна 4, 5 или 6")
    else:
        break
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/5.png)

### Вывод: В данном коде благодаря циклу for и условиям меняется значение переменной i до того, пока цикл не закончится.

### №6
### Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string.
### Ответ:
```python
string = "Привет всем изуающим Python!"
value = input()
for i in string:
    if i == value:
        index = string.find(value)
        print(f"Буква '{value}' есть в строке под {index} индексом")
        break
else:
    print(f"Буквы '{value}' нет в указанной строке")
```
![Меню](http://github.com/TanyaTolm/PI/blob/Theme3/pic/6.png)

### Вывод: В данном коде используется поиск переменной с помощью индекса и метода find.

### №7
### Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть к примеру не от  0 до 10, а от 10 до 0.

### Ответ:
```python
value = 100
for i in range(10, -1, -1):
    value -= i
    print(i, value)
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/7.png)

### Вывод: В данном коде наглядно показано как работает цикл for, как обрабатывается переменная и последовательно выполняются действия в коде.

### №8
### Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, посмольку циклы while при неправильно написанных условиях могут становится бесконечными, как указано в примере далее.

### Ответ:
```python
value = 0
while value < 100:
    if value == 0:
        value += 10
    elif value // 5 > 1:
        value  *= 5
    else:
        value -= 5
    print(value)
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/8.png)

### Вывод: В данном коде продемонстрировано как работает цикл while. Без грамотно написанных условий этот цикл может стать бесконечным.

### №9
### Напишите программу с использованием вложенных циклов и одной проверкой внутри них. Самое главное, не забудьте, что нельзя использовать одинаковые имена итерируемых переменных, когда вы используете вложенные циклы.

### Ответ:
```python
value = 0
for i in range(10):
    for j in range(10):
        if i != j:
            value += j
        else:
            pass
print(value)
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/9.png)

### Вывод: В данном коде продемонстрировано как работают вложеные циклы. Важно помнить, что нельзя использовать одинаковые переменные для циклов.

### №10
### Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве, четных чисел.

### Ответ:
```python
even_array = [2, 4, 6, 8, 9]
flag = False
for value in even_array:
    if value % 2 == 1:
        flag = True

if flag is True:
    print("В массиве есть нечетное число")
else:
    print("В массиове есть числа четные")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/10.png)

### Вывод: В данном коде показано как можно пользоваться flag для определения выполнения условия в цикле for.

## Самостоятельная работа

### №1
### Напишите программу, которая преобразует 1 в 31. Для выполнения поставленной задачи необходимо обязательно и только один раз использовать:
### • Цикл for
### • *=5
### += 1
### Никаких других действий или циклов использовать нельзя.

### Ответ:
```python
for i in range (7):
    i *= 5
    i += 1
print(i)
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/11.png)

### Вывод: В данном коде показано как работает цикл и как последовательно исполняется каждое действие в цикле.

### №2 
### Напишите программу, которая фразу «Hello World» выводит в обратном порядке, и каждая буква находится в одной строке консоли.

### Ответ:
```python
a = "Hello World"
for i in range (10, -1, -1):
    print(a[i])
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/12.png)

### Вывод: В данном коде показано как с помощью цикла for и индекса можно пройти по строчке и вывести каждый символ в консоль.

### №3 
### Напишите программу, на вход которой поступает значение из консоли, оно должно быть числовым и в диапазоне от 0 до 10 включительно (это необходимо учесть в программе). Если вводимое число не подходит по требованиям, то необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять в каком диапазоне находится полученное число. Нужно учитывать три диапазона:
### от 0 до 3 включительно
### • от 3 до 6
### • от 6 до 10 включительно
### Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строчек в редакторе кода.

### Ответ:
```python
a = int(input("Введите число от 0 до 10: "))
if a < 0 or a > 10:
    print("Введенное число не подходит, попробуйте снова")
    exit()
if a >= 0 and a <= 3: print("Диапазон от 0 до 3 включительно")
elif a > 3 and a < 6: print("Диапазон от 3 до 6")
elif a >= 6 and a <= 10: print("Диапазон от 6 до 10 включительно")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/13.png)

### Вывод: В данном коде с помощью elif мы определяем диапазон числа, а также проверем, чтобы число соответствовало условию задачи. 

### №4
### Манипулирование строками. Напишите программу на Python, которая принимает предложение (на английском) в качестве входных данных от пользователя. Выполните следующие операции и отобразите результаты:
### • Выведите длину предложения.
### • Переведите предложение в нижний регистр.
### • Подсчитайте количество гласных (а, е, і, о, и) в предложении.
### • Замените все слова "ugly" на "beauty".
### • Проверьте, начинается ли предложение с "The" и заканчивается ли на "end".
### Проверьте работу программы минимум на 3 предложениях, чтобы охватить проверку всех поставленных условий.

### Ответ:
```python
a = "THE HOUSE IS UGLY. END"
count = 0
flag = False
g = ["a", "e", "i", "o", "u", "A", "E", "I", "O", "U"]
for i in range (len(g)):
    for j in range(len(a)):
        if g[i] == a[j]:
            count += 1

if ((a.startswith("THE") and a.endswith("END")) or
    (a.startswith("The") and a.endswith("end"))):
    print("Строка начинается с The и заканчивается end")

if "UGLY" in a:
    a = a.replace("UGLY", "beauty")
    print(f"В предложении заменены все ugly на beauty: {a}")


print(f"Длина строки равна {len(a)}")
print(f"Предложение в нижнем регистре: {a.lower()}")
print(f"Количество гласных в предложении: {count}")
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/14.png)

### Вывод: В данном коде используется несколько функций и методов:
- startswith и endswith: определяет совпадает начало и конец строки с условием.
- replace: заменяет одно на другое.
- len: выдает длинну строки.
- lower: переводит строку в нижний регистр.

### №5 
### Составьте программу, результатом которой будет данный вывод в консоль, используя строки только один раз. Не обязательно использовать все строки.

### Ответ:
```python
string = "hello"
memory = " world"
values = [0, 2, 4, 6, 8, 10]
counter = 0
while counter != 10:
    if counter in values:
        print(string + memory)
        print(string)
    counter += 1
print(string + memory)
```
![Меню](https://github.com/TanyaTolm/PI/blob/Theme3/pic/15.png)

### Вывод: В данном коде мы к строке hello добавляем world, при соблюдении определенных условий.

### Общий вывод по теме:

В этой лабораторной и самостоятельной работе мы познакомились с использованием операторов, условий и циклов в коде на языке программирования Python. Например, условный оператор if, elif, else, цикл while, in range(), цикл for и другие.
